---
clusterName: 'cluster.name.local'
namespaces:
  monitoring:
    daemonsets:
      - "prometheus-operator-prometheus-node-exporter"
    services:
      - "prometrhes"
      - "alertmanager"
    secrets:
      - "test"
  opa:
  kube-system:
  logging:
    daemonsets:
      - "fluent-bit"
  kiam:
    daemonsets:
      - "kiam-server"
      - "kiam-agent"
nginxIngress:
    namespacePrefix: "smoketest-ingress-"
